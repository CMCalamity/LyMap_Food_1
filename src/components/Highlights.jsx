import { useEffect,useState } from 'react'
import { Play, Quote } from 'lucide-react'
import Reveal from './Reveal'
import {getSiteContent} from '../utils/storage'
function Video({url,title}){if(!url)return null;const yt=url.match(/(?:youtube\.com\/watch\?v=|youtu\.be\/)([^&?/]+)/);if(yt)return <div className="absolute inset-0"><iframe className="w-full h-full" src={`https://www.youtube.com/embed/${yt[1]}`} title={title||'Video Lý Mập Food'} allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowFullScreen/></div>;return <video className="absolute inset-0 w-full h-full object-cover" src={url} controls playsInline/>}
export default function Highlights(){const[c,setC]=useState(getSiteContent);useEffect(()=>{const r=()=>setC(getSiteContent());r();window.addEventListener('lymap-content',r);return()=>window.removeEventListener('lymap-content',r)},[]);const cards=c.highlights?.cards||[];return <section id="highlights" className="py-16 md:py-24 px-margin-mobile md:px-margin-desktop bg-surface-container-low"><div className="max-w-container-max mx-auto"><div className="flex flex-col md:flex-row md:items-end justify-between gap-4 mb-9"><div><span className="text-primary text-[10px] font-black uppercase tracking-[.22em]">Bảo chứng của quán</span><h2 className="font-headline-md text-headline-md text-on-surface mt-2">{c.highlights?.title}</h2></div><Quote className="w-9 h-9 text-primary opacity-40"/></div><div className="grid grid-cols-1 md:grid-cols-12 gap-4">{cards.map((x,i)=><Reveal key={x.title+i} className={`${i===0?'md:col-span-7 md:row-span-2':'md:col-span-5'} relative min-h-[230px] rounded-[28px] overflow-hidden bg-surface-container-lowest border border-tertiary-fixed-dim p-7 flex flex-col justify-end`}>
{(x.video||x.image)&&<><div className="absolute inset-0 opacity-25">{x.video?<Video url={x.video} title={x.title}/>:<img src={x.image} className="w-full h-full object-cover" alt=""/>}</div><div className="absolute inset-0 bg-gradient-to-t from-black/80 via-black/20 to-transparent"/></>}
<div className="relative z-10">{x.video&&!x.image&&<div className="w-9 h-9 rounded-full bg-primary text-on-primary flex items-center justify-center mb-3"><Play className="w-4 h-4"/></div>}<span className="material-symbols-outlined text-primary text-3xl mb-3">{x.icon||'star'}</span><h3 className={`font-headline-sm text-headline-sm ${x.video||x.image?'text-white':'text-on-surface'}`}>{x.title}</h3><p className={`text-xs mt-2 max-w-lg ${x.video||x.image?'text-white/80':'text-on-surface-variant'}`}>{x.text}</p></div></Reveal>)}</div></div></section>}
