import { useEffect,useState } from 'react'; import { getSiteContent } from '../utils/storage'
export default function Story(){const [c,setC]=useState(getSiteContent);useEffect(()=>{const r=()=>setC(getSiteContent());r();window.addEventListener('lymap-content',r);return()=>window.removeEventListener('lymap-content',r)},[]);const x=c.story||{};return <section id="story" className="py-20 px-margin-mobile md:px-margin-desktop bg-surface-container-lowest border-y border-surface-container-high"><div className="max-w-container-max mx-auto grid md:grid-cols-12 gap-12 items-center"><div className="md:col-span-5 relative rounded-[32px] overflow-hidden soft-shadow aspect-square bg-tertiary-fixed"><img src={x.image} alt={x.title} className="w-full h-full object-cover"/><div className="absolute inset-0 bg-gradient-to-t from-black/60 via-transparent to-transparent flex items-end p-6"><span className="text-white text-xs font-bold bg-primary/90 backdrop-blur px-3 py-1.5 rounded-full">{x.badge}</span></div></div><div className="md:col-span-7 space-y-4"><span className="inline-block bg-primary-fixed text-on-primary-fixed-variant px-4 py-1 rounded-full font-label-sm text-label-sm uppercase tracking-wider">{x.eyebrow}</span><h2 className="font-headline-md text-headline-md text-primary">{x.title}</h2><p className="font-body-md text-body-md text-on-surface-variant leading-relaxed">{x.text}</p><div className="grid sm:grid-cols-2 gap-4 pt-2">{(x.cards||[]).map(card=><div key={card.title} className="p-5 rounded-2xl bg-surface-container-low border border-tertiary-fixed-dim"><h4 className="font-headline-sm text-primary text-base mb-1">{card.title}</h4><p className="text-xs text-on-surface-variant">{card.text}</p></div>)}</div></div></div></section>}
