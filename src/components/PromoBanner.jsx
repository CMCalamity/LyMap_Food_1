import {useEffect,useState} from 'react'
import {getSiteContent} from '../utils/storage'
export default function PromoBanner(){const [c,setC]=useState(getSiteContent);useEffect(()=>{const r=()=>setC(getSiteContent());window.addEventListener('lymap-content',r);return()=>window.removeEventListener('lymap-content',r)},[]);const p=c.promo||{};if(p.enabled===false)return null;return <section className="px-margin-mobile md:px-margin-desktop py-5"><div className="max-w-container-max mx-auto rounded-3xl bg-primary text-white px-5 py-5 sm:px-7 flex flex-col sm:flex-row sm:items-center justify-between gap-4 shadow-lg"><div><div className="flex items-center gap-2"><span className="text-[9px] font-black uppercase bg-white/15 px-2 py-1 rounded-full">{p.badge||'HOT'}</span><span className="text-[10px] font-bold opacity-80">{p.eyebrow||'Ưu đãi hôm nay'}</span></div><h2 className="text-lg sm:text-xl font-black mt-1">{p.title}</h2><p className="text-xs opacity-80 mt-1 max-w-xl">{p.text}</p></div><a href="#combos" className="shrink-0 bg-white text-primary px-4 py-2.5 rounded-full text-xs font-black">{p.cta||'Xem ngay'}</a></div></section>}